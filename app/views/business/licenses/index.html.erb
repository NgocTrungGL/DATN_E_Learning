<h2 class="fw-bold text-primary mb-4"><i class="bi bi-ticket-perforated me-2"></i>Course license inventory</h2>

<% if @licenses_by_course.any? %>
  <div class="row">
    <% @licenses_by_course.each do |course, licenses| %>
      <%
         total_count = licenses.count
         available_count = licenses.select(&:available?).count
         assigned_count = total_count - available_count
         progress_percent = (assigned_count.to_f / total_count * 100).round
      %>

      <div class="col-md-6 mb-4">
        <div class="card shadow-sm border-0 h-100">
          <div class="card-body">
            <div class="d-flex align-items-start justify-content-between mb-3">
              <div>
                <h5 class="fw-bold mb-1"><%= course.title %></h5>
                <span class="badge bg-light text-dark border">Value: <%= number_to_currency(licenses.first.price) %>/seat</span>
              </div>
              <% if available_count > 0 %>
                <span class="badge bg-success"><%= available_count %> seats available</span>
              <% else %>
                <span class="badge bg-secondary">Sold out</span>
              <% end %>
            </div>

            <div class="mb-3">
              <div class="d-flex justify-content-between small text-muted mb-1">
                <span>Used: <%= assigned_count %></span>
                <span>Total: <%= total_count %></span>
              </div>
              <div class="progress" style="height: 6px;">
                <div class="progress-bar" role="progressbar" style="width: <%= progress_percent %>%"></div>
              </div>
            </div>

            <hr>

            <% if available_count > 0 %>
              <div class="mt-3">
                <label class="small fw-bold text-muted mb-2">Assign to employee:</label>

                <%= form_with url: assign_business_licenses_path, method: :post, class: "d-flex gap-2" do |f| %>
                  <%= f.hidden_field :course_id, value: course.id %>

                  <%= f.collection_select :user_id, current_user.organization.users.where(role: :employee), :id, :name, { prompt: "Select employee..." }, { class: "form-select form-select-sm", required: true } %>

                  <%= f.button type: "submit", class: "btn btn-sm btn-primary text-nowrap" do %>
                    <i class="bi bi-check-lg"></i> Assign now
                  <% end %>
                <% end %>
              </div>
            <% else %>
              <div class="alert alert-warning small mb-0">
                <i class="bi bi-exclamation-circle me-1"></i> You have used all licenses for this course. Please purchase more.
              </div>
            <% end %>

            <div class="mt-3">
                <a class="btn btn-link btn-sm text-decoration-none p-0" data-bs-toggle="collapse" href="#list-<%= course.id %>">
                  View assigned list <i class="bi bi-chevron-down"></i>
                </a>
                <div class="collapse mt-2" id="list-<%= course.id %>">
                  <ul class="list-group list-group-flush small">
                    <% licenses.select(&:assigned?).each do |lic| %>
                      <li class="list-group-item px-0 d-flex justify-content-between bg-transparent">
                        <span><i class="bi bi-person me-2"></i><%= lic.user&.name %></span>
                        <span class="text-muted"><%= lic.updated_at.strftime("%d/%m") %></span>
                      </li>
                    <% end %>
                  </ul>
                </div>
             </div>

          </div>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="text-center py-5">
    <div class="display-1 text-muted mb-3"><i class="bi bi-cart-x"></i></div>
    <h4>You have not purchased any licenses yet.</h4>
    <p class="text-muted">Contact the system admin to purchase a training package.</p>
  </div>
<% end %>
