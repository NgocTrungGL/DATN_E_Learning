<div class="modal fade" id="questionModal" tabindex="-1" aria-labelledby="questionModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg"> <div class="modal-content border-0 shadow-lg">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title fw-bold" id="questionModalLabel">
          <i class="bi bi-chat-square-text-fill me-2"></i> Ask a discussion question
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <div class="modal-body p-4 bg-light">
        <div class="d-flex mb-4 align-items-center">
          <div class="flex-shrink-0 me-3">
             <img src="https://ui-avatars.com/api/?name=<%= current_user&.name || 'User' %>&background=random&color=fff&background=0d6efd" class="rounded-circle shadow-sm" width="50" height="50">
          </div>
          <div>
            <h6 class="fw-bold mb-0"><%= current_user&.name %></h6>
            <small class="text-muted">Asking a question about lesson: <strong><%= @lesson.title %></strong></small>
          </div>
        </div>

        <%= form_with(model: [@lesson, Comment.new], local: true) do |f| %>
          <div class="mb-3">
            <%= f.label :body, "Question details", class: "form-label fw-bold text-secondary" %>
            <%= f.text_area :body, class: "form-control shadow-sm border-0 p-3", placeholder: "Describe your question in detail so the instructor and others can help...", rows: 5, style: "resize: none;" %>
          </div>

          <div class="alert alert-info d-flex align-items-center small py-2" role="alert">
            <i class="bi bi-info-circle-fill flex-shrink-0 me-2"></i>
            <div>
              Please use polite and respectful language.
            </div>
          </div>

          <div class="d-flex justify-content-end gap-2 mt-4">
            <button type="button" class="btn btn-secondary px-4" data-bs-dismiss="modal">Cancel</button>
            <%= f.submit "Submit question", class: "btn btn-primary px-4 fw-bold shadow-sm" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
