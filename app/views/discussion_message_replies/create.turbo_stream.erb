<%# Reset the reply form after submission %>
<%= turbo_stream.replace "reply-form-#{@parent.id}" do %>
  <%= render "discussion_message_replies/form", course: @course, parent: @parent, reply: DiscussionMessage.new %>
<% end %>
